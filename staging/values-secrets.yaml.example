# Copy to values-secrets.yaml and fill in — never commit values-secrets.yaml
# Apply with: helm upgrade gitea-stage ... -f staging/values.yaml -f staging/values-secrets.yaml
gitea:
  config:
    "oauth2_client.authentik":
      CLIENT_ID: ""     # From Authentik staging gitea-stage application
      CLIENT_SECRET: "" # From Authentik staging gitea-stage application
